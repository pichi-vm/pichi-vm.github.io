---
// ── How it works ──────────────────────────────────────
import { landing } from "../content/schema";
import { Content as Intro } from "../content/prose/how-it-works-intro.md";
import Steps from "./Steps.astro";

const { howItWorks } = landing;
---

<section class="section">
  <h2 class="h">{howItWorks.heading}</h2>
  <div class="section-desc hiw-intro">
    <Intro />
  </div>
  <Steps steps={howItWorks.steps} />
  <div class="fanout">
    <div class="fan-img">{howItWorks.fanout.image}</div>
    <div class="fan-arrows" aria-hidden="true">
      ↙ ↓ ↘
    </div>
    <div class="fan-targets">
      {howItWorks.fanout.targets.map((t) => (
        <span class="fan-t">{t}</span>
      ))}
    </div>
  </div>
</section>

<style>
  .hiw-intro {
    text-align: center;
    margin-bottom: 1.75rem;
  }
  .fanout {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.9rem;
    padding: 1.75rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
  }
  .fan-img {
    font-family: var(--font-mono);
    font-weight: 700;
    color: #fff;
    background: var(--bg);
    border: 1px solid var(--accent);
    border-radius: 8px;
    padding: 0.6rem 1.1rem;
    font-size: 0.9rem;
  }
  .fan-arrows {
    color: var(--muted);
    font-size: 1.2rem;
    letter-spacing: 0.5rem;
  }
  .fan-targets {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  .fan-t {
    font-size: 0.85rem;
    color: var(--fg);
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.35rem 0.9rem;
  }
</style>
